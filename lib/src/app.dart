import 'package:flutter_web/material.dart';
import 'screens/column_row_sample.dart';

class App extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
        title: 'Column Sample',
        home: ColumnRowSample(),
    );
  }
}
